global:
  scrape_interval: 1s
scrape_configs:
  - job_name: calculator
    metrics_path: /metrics
    static_configs:
      # For Prometheus in the same Compose network.
      # For a host process use localhost:8080 instead.
      - targets: ["calculator:8080"]
